using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
 * For each individual(datapoint) we have the id, when(timestamp) it goes to where(lng,lat,height)
 */
public class Individual
{ 
    
    public string id;
    public string timestamp;
    public string lng;
    public string lat;
    public float height;
   
}
